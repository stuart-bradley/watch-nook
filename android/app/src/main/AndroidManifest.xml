<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app's one network dependency is the metadata API (TMDB). Flutter's
         scaffold declares INTERNET only in the debug/profile manifests, so it
         MUST be declared here too — otherwise the RELEASE build (the one users
         download) ships with no network access at all and every request fails
         "Failed host lookup". This is invisible to debug/profile builds and to
         `just check`; only a release build on a device surfaces it. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- allowBackup=true enables Android Auto Backup (US-11). Both rule files are
         ALLOWLISTS (ADR-3): only <filesDir>/backup/ — the user-owned export JSON —
         leaves the device. Drop either attribute and Android reverts to its
         default, which backs up databases/ (the disposable cache) and
         shared_prefs/ (the metadata API key). backup_rules is API <=30,
         data_extraction_rules is API 31+; both are required. -->
    <application
        android:label="Watchnook"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
