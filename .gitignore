# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
coverage/

# Generated code (regenerated by build_runner — never commit)
*.g.dart
*.freezed.dart
*.mocks.dart

# Patrol (generated; excluded from analysis — never commit)
integration_test/test_bundle.dart
patrol_test/

# Android
android/.gradle/
android/key.properties
android/app/debug/
android/app/profile/
android/app/release/
**/*.keystore
**/*.jks

# Local secrets / config (API keys etc. never committed)
*.env
secrets.json

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# Local agent/dev-tooling scratch (not part of the app)
.autopilot/
.autopilot.json
/MORNING.md
/STOP
